import React from 'react';
import App from './App';
const manipulate = (props) => {
    
        if (this.refs.myInput !== null) {
            var input = this.refs.props.myInput;
                var inputValue = input.value;
          alert("Input is " + inputValue);
        }
      }
export default manipulate;